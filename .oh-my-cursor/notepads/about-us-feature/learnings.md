# About Us feature (CLI)

- Added `oh-my-cursor about` in `bin/oh-my-cursor.mjs`: prints version from `package.json`, short project blurb, doc paths, Team artifact locations, upstream credit + NOTICE pointer, MIT license.
- `about` ignores workspace; no spawn to sub-scripts.
